# uet-2526i-int3405e-4-finals-MALLORN-astronomical-classification-challenge
> https://www.kaggle.com/competitions/mallorn-astronomical-classification-challenge/

## How to install
```cmd
$ poetry install

# Windows
$ cmd /c "set KAGGLE_USERNAME=<KAGGLE_USERNAME> && set KAGGLE_KEY=<KAGGLE_KEY> && poetry run kaggle competitions download --competition \"mallorn-astronomical-classification-challenge\" --path \"./resources\" --unzip"
$ cmd /c "set KAGGLE_USERNAME=ducknt && set KAGGLE_KEY=886a37dac21def5de49fd2debc0eadeb && poetry run kaggle competitions download --competition \"mallorn-astronomical-classification-challenge\" --path \"./resources\" --unzip"
$ cmd /c "set KAGGLE_USERNAME="ducknt" && set KAGGLE_KEY="886a37dac21def5de49fd2debc0eadeb" && poetry run kaggle competitions download --competition mallorn-astronomical-classification-challenge --path ./resources"

# MacOS/Linux
```

## How to run
...
